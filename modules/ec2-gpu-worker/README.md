# `ec2-gpu-worker` (stub)

GPU EC2 instance for asset processing — Gaussian splat training, video encoding, ML inference.

**Status: skeleton only.** Implement on first project that needs GPU compute. See `task_plan.md` Phase 6.
